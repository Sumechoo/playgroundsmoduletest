import SwiftUI
//import CppTest

struct ContentView: View {
    init() {
//        CppTest.doLog()
    }
    
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
    }
}
